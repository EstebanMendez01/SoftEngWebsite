<section class="py-16 bg-dark text-light flex flex-col items-center justify-center text-center">
    <h2 class="text-3xl font-bold mb-8">Skills & Technologies</h2>
    <div class="wheel-3d-container">
      <div class="wheel-3d">
        <img src="https://cdn.simpleicons.org/html5" alt="HTML5" class="logo" />
        <img src="https://cdn.simpleicons.org/css3" alt="CSS3" class="logo" />
        <img src="https://cdn.simpleicons.org/javascript" alt="JavaScript" class="logo" />
        <img src="https://cdn.simpleicons.org/react" alt="React" class="logo" />
        <img src="https://cdn.simpleicons.org/nodedotjs" alt="Node.js" class="logo" />
        <img src="https://cdn.simpleicons.org/python" alt="Python" class="logo" />
        <img src="https://cdn.simpleicons.org/postgresql" alt="PostgreSQL" class="logo" />
        <img src="https://cdn.simpleicons.org/mongodb" alt="MongoDB" class="logo" />
        <img src="https://cdn.simpleicons.org/docker" alt="Docker" class="logo" />
      </div>
    </div>
  </section>
  
  <style>
    .bg-dark {
      background-color: #1a1a1a; /* Replace with the actual background color from Experience.tsx */
    }

    .text-light {
      color: #ffffff; /* Replace with the actual font color from Experience.tsx */
    }

    .wheel-3d-container {
      perspective: 1000px;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 450px;
    }
  
    .wheel-3d {
      width: 350px;
      height: 350px;
      position: relative;
      transform-style: preserve-3d;
      animation: spinWheel 20s linear infinite;
    }
  
    .logo {
      position: absolute;
      width: 80px;
      height: 80px;
      left: 50%;
      top: 50%;
      transform-origin: center center;
      opacity: 0.9;
      transition: transform 0.3s ease;
    }
  
    .logo:hover {
      opacity: 1;
      transform: scale(1.1) translateZ(200px);
    }
  
    .logo:nth-child(1)  { transform: rotateY(0deg) translateZ(200px); }
    .logo:nth-child(2)  { transform: rotateY(40deg) translateZ(200px); }
    .logo:nth-child(3)  { transform: rotateY(80deg) translateZ(200px); }
    .logo:nth-child(4)  { transform: rotateY(120deg) translateZ(200px); }
    .logo:nth-child(5)  { transform: rotateY(160deg) translateZ(200px); }
    .logo:nth-child(6)  { transform: rotateY(200deg) translateZ(200px); }
    .logo:nth-child(7)  { transform: rotateY(240deg) translateZ(200px); }
    .logo:nth-child(8)  { transform: rotateY(280deg) translateZ(200px); }
    .logo:nth-child(9)  { transform: rotateY(320deg) translateZ(200px); }
  
    @keyframes spinWheel {
      0%   { transform: rotateY(0deg); }
      100% { transform: rotateY(360deg); }
    }
  </style>
  